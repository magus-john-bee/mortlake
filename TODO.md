# Mortlake TODO

- [ ] Try Logseq 2 (DB version) via [Bad3r/nix-logseq-git-flake](https://github.com/Bad3r/nix-logseq-git-flake) — side-by-side with OG on a throwaway test graph. Two-way markdown sync still draft (PR #12593), so don't point at real vault. Watch nixpkgs PR #516682 for `logseq_2` to eventually drop the flake.
- [ ] **Cognee + Postgres for Pi memory** — [plan](./docs/cognee-setup-plan.md). Single Postgres (relational + pgvector + graph tables), fastembed local embeddings, GLM via Z.AI for entity extraction. systemd user service on Uriel, REST API on :8000, Pi calls via curl.
- [ ] **VFIO SteamOS VM on Jehoel** — [plan](./docs/vfio-steamos-plan.md). Pass through the 5700 XT eGPU + a USB controller to a SteamOS KVM guest via VFIO. Solves the NixOS async reprojection / CAP_SYS_NICE blocker for SteamVR. Primary use case: Steam Frame VR streaming when the headset ships. Hardware is ideal (780M iGPU for host, UM780 XTX has clean IOMMU groups). Not blocked until Steam Frame launches — can validate with SteamVR + a tethered headset or Remote Play first.
- [ ] **Parallel Pi agent orchestration with herdr + jj** — [Detailed plan](./docs/agent-coordination-plan.md). Three layers: jj workspaces + bookmark chains (structural DAG, auto-rebase), OpenSpec (spec content), `COORDINATION.md` (plaintext claim/status/handoff). No external task manager — evaluated lodestar/claimd/beans/hence, all overbuilt for <12 agents. Claims via `mkdir .claims/<id>` (POSIX atomic). Stale cleanup via herdr post-exit hook. Phased rollout: conventions → helper scripts → herdr integration → OpenSpec wiring → operational validation.
- [ ] **KeePassXC Syncthing conflict merge cron** — Set up a cron job (or systemd timer) on Jehoel that scans the Syncthing-synced KeePass folder for `.sync-conflict-*.kdbx` files and auto-merges them into the main DB using `keepassxc-cli merge --same-credentials`. Reference: [michaelharley.net script](https://michaelharley.net/posts/2026/05/23/my-keepass-and-syncthing-setup/). Also enable Syncthing staggered file versioning on the KeePass folder for rollback safety. Depends on KeePassXC → Proton Pass migration being done first (import unencrypted JSON export, re-register passkeys).
- [ ] **PocketBook InkPad One: SSH + Syncthing** — Sideload file-transfer tooling onto the e-reader (PB1030, Linux 4.9.56, RK3566). Both are `.app` drops into `applications/` over USB, no root: (1) Dropbear SSH from [bjw-s-labs/pocketbook-ssh](https://github.com/bjw-s-labs/pocketbook-ssh) — ssh/scp as `reader`, use `scp -O` (Dropbear has no SFTP subsystem); (2) Syncthing via [mikedigriz/Syncthing-for-PocketBook](https://github.com/mikedigriz/Syncthing-for-PocketBook) — needs `setsid`-detached wrapper (naive one freezes UI) + poke `/ebrmain/bin/scanner.app` after sync or new files don't appear in the library; PC folder set Send Only (one-way book push, no conflict files). Gotchas: PocketBook sleeps Wi-Fi aggressively (sync only happens while radio is awake — fine for "syncs when picked up"); verify userspace arch (32-bit ARM vs aarch64) before grabbing binaries — pbterm (`file /bin/busybox`) answers it, segfault means wrong tarball. Reference: [tastytea guide](https://blog.tastytea.de/posts/syncthing-on-pocketbook/).
- [ ] **Reify Ouroboros novel concepts as OpenSpec-complementary skills** — Ouroboros dropped as a system (replaced by OpenSpec), but several concepts are worth extracting into standalone skills that compose with OpenSpec's `/opsx:*` workflow: (1) **goal-drift measurement** — quantify how far implementation has drifted from spec during/after `/opsx:apply`; (2) **three-stage verification pipeline** — structured QA (spec compliance → code quality → behavioral correctness) to run before `/opsx:archive`; (3) **Socratic requirements interview** — guided questioning to crystallize vague requirements before `/opsx:propose`; (4) **lateral thinking / "unstuck"** — persona-based prompt engineering to break through stagnation; (5) **evolutionary convergence loop** — auto-iterate from goal to working artifact with status tracking and convergence measurement.
